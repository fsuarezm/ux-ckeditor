{% block ckeditor_widget -%}
    {%- set dataController = (attr['data-controller']|default('') ~ ' fsm--ux-ckeditor--ckeditor')|trim -%}
{#    {%- set dataController = (attr['data-controller']|default('') ~ ' xearts--symfony-ux-ckeditor5--ckeditor5')|trim -%}#}

    {%- set attr = attr|merge({
        'data-controller': dataController,
        class: (attr.class|default('') ~ ' ckeditor-textarea')|trim,
        'data-fsm--ux-ckeditor--ckeditor-language-value': language,
        'data-fsm--ux-ckeditor--ckeditor-config-value': ckeditor_options,
    }) -%}

    {{ block('textarea_widget') }}
{%- endblock %}
