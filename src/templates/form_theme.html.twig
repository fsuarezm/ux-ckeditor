{% block ckeditor_widget -%}
    {%- set dataController = (attr['data-controller']|default('') ~ ' fsuarezm--ux-ckeditor--ckeditor')|trim -%}
    {%- set attr = attr|merge({
        'data-controller': '',
        class: (attr.class|default('') ~ ' ckeditor-textarea')|trim,
        'data-fsuarezm--ux-ckeditor--ckeditor-language-value': language,
        'data-fsuarezm--ux-ckeditor--ckeditor-config-value': ckeditor_options,
    }) -%}

    {{ block('textarea_widget') }}
{%- endblock %}